import { ChangeArrowClassDirective } from './change-arrow-class.directive';

describe('ChangeArrowClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeArrowClassDirective();
    expect(directive).toBeTruthy();
  });
});
